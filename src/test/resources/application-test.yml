spring:
  datasource:
    url: jdbc:h2:mem:ems_test;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

app:
  jwt:
    secret: test-secret-key-for-unit-tests-only-minimum-256-bits-long-value-here
    expiration-ms: 3600000
